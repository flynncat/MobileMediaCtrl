<Window x:Class="MediaBrowser.App.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="900"
        Height="700"
        WindowStartupLocation="CenterScreen"
        Background="Black"
        KeyDown="Window_KeyDown">
  <Grid>
    <!-- ͼƬԤ�� -->
    <Image x:Name="ImageViewer"
           Stretch="Uniform"
           Margin="8"
           Visibility="Collapsed"/>
    <!-- ��Ƶ���� -->
    <MediaElement x:Name="VideoPlayer"
                  LoadedBehavior="Manual"
                  UnloadedBehavior="Stop"
                  Stretch="Uniform"
                  Margin="8"
                  Visibility="Collapsed"
                  MediaEnded="VideoPlayer_MediaEnded"/>
    <!-- ������ʾ -->
    <TextBlock x:Name="LoadingText"
               Text="���ڼ���Ԥ����"
               Foreground="White"
               FontSize="18"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"/>
    <!-- �ײ�����������Ƶ�ã� -->
    <StackPanel x:Name="VideoControls"
                Orientation="Horizontal"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="0,0,0,16"
                Visibility="Collapsed">
      <Button Content="��ͣ/����" Click="PlayPause_Click"

              Foreground="White" Background="#444" Padding="12,6" Margin="4"
              FontSize="14"/>
      <Button Content="�ر�" Click="Close_Click"

              Foreground="White" Background="#444" Padding="12,6" Margin="4"
              FontSize="14"/>
    </StackPanel>
  </Grid>
</Window>
